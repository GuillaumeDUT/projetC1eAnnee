#include <stdio.h>
#include <stdlib.h>

#include "structure.h"


int main(){
  Monde monde;
  gererPartie(&monde);

  // afficherGrille(&monde);
  // afficherListe(monde.rouge);
  // afficherListe(monde.bleu);
  return 0;
}
